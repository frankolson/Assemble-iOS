platform :ios, '14.1'

target 'Assemble' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Assemble
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  # pod 'FirebaseUI'
end
